CXX_STD      = CXX11
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_CPPFLAGS = -I. -Imio 

PKG_LIBS     = $(SHLIB_OPENMP_CXXFLAGS) -L. -lMIO

# libraries
LIBMIO  = mio/mio.o
$(SHLIB): libMIO.a

libMIO.a: $(LIBMIO)
	$(AR) rcs libMIO.a $(LIBMIO)

clean:
	rm -f $(SHLIB) $(OBJECTS) $(LIBMIO) libMIO.a 
