
message("TODO: provide options for method `mature` not to output filearrays")
message("TODO: add operator type to output inconsistent dimensions")
message("TODO: make function `fmap` work for proxy arrays")
message("TODO: add test functions for `fmap` on proxy arrays")
message("TODO: use `fmap` or `fmap2` in `mature` instead of pairwise one")
message("TODO: add test functions for `fmap` on proxy arrays")

message("TODO: add methods & test for `[` and `[<-` for proxy arrays")

message("TODO: add test functions for `apply` on proxy arrays")
